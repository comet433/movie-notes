<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Movie</title>
</head>
<body>
  <h1>Edit Movie</h1>

  <form action="/movies/<%= movie.id %>" method="POST">
    <label>Title: <input name="title" value="<%= movie.title %>" required /></label><br/>
    <label>Director: <input name="director" value="<%= movie.director %>" /></label><br/>
    <label>Rating: <input type="number" name="rating" min="1" max="10" value="<%= movie.rating %>" /></label><br/>
    <label>Date Watched: <input type="date" name="date_watched" value="<%= movie.date_watched.toISOString().slice(0, 10) %>" /></label><br/>
    <label>Notes:<br/>
      <textarea name="notes" rows="4" cols="40"><%= movie.notes %></textarea>
    </label><br/><br/>
    <button type="submit">Update Movie</button>
  </form>

  <a href="/movies">← Back to Movie List</a>
</body>
</html>
